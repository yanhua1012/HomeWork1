@model IEnumerable<HomeWork1.Models.BalanceEntry>
@{
    Layout = null;
}
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-hover">
            <tr>
                <th>#</th>
                <th>類別</th>
                <th>日期</th>
                <th>金額</th>
            </tr>
            @{
                int sernalNo = 0;
                foreach (var item in Model)
                {
                    <tr>
                        <td>@(++sernalNo)</td>
                        <td>@item.CategoryText</td>
                        <td>@item.Date.ToString("yyyy-MM-dd")</td>
                        <td>@item.Money</td>
                    </tr>
                }
            }
        </table>
    </div>
</div>